services:
  resolver:
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: ["resolver"]
    env_file:
      - .env
    environment:
      ONT_RESOLVER_PORT: "8787"
      ONT_SNAPSHOT_PATH: /app/.data/resolver-snapshot.json
      ONT_VALUE_STORE_PATH: /app/.data/value-records.json
    ports:
      - "${ONT_RESOLVER_BIND:-127.0.0.1}:${ONT_RESOLVER_PORT:-8787}:8787"
    volumes:
      - ont_data:/app/.data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8787/health >/dev/null || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 10s

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: ["web"]
    env_file:
      - .env
    environment:
      ONT_WEB_PORT: "3000"
      ONT_WEB_RESOLVER_URL: http://resolver:8787
      ONT_RESOLVER_PORT: "8787"
      ONT_WEB_PRIVATE_SIGNET_FUNDING_ENABLED: "${ONT_WEB_PRIVATE_SIGNET_FUNDING_ENABLED:-false}"
    depends_on:
      resolver:
        condition: service_healthy
    ports:
      - "${ONT_WEB_BIND:-127.0.0.1}:${ONT_WEB_PORT:-3000}:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:3000/api/health >/dev/null || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 10s

  indexer:
    profiles: ["ops"]
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: ["indexer"]
    env_file:
      - .env
    environment:
      ONT_SNAPSHOT_PATH: /app/.data/indexer-snapshot.json
      ONT_SNAPSHOT_KEY: indexer
    volumes:
      - ont_data:/app/.data

volumes:
  ont_data:
