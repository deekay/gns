{
  "name": "@ont/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx --no-cache src/index.ts",
    "test": "npm run build -w @ont/protocol && npm run build -w @ont/core && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ont/architect": "file:../../packages/architect",
    "@ont/core": "file:../../packages/core",
    "@ont/protocol": "file:../../packages/protocol",
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^1.8.0",
    "buffer": "^6.0.3",
    "esbuild": "^0.25.12"
  }
}
