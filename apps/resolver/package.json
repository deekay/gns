{
  "name": "@ont/resolver",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ont/core": "file:../../packages/core",
    "@ont/db": "file:../../packages/db",
    "@ont/protocol": "file:../../packages/protocol"
  }
}
